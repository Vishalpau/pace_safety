/**
 * @file - CardFunctions.js
 * @location /app/components/Card
 * @description Includes all the functions of Card Component
 * @author Abhimanyu<abhimanyus@teknobuilt.com>
 * @since v1.1.0
 **/

// Function to go to detail page.

export const handleSummaryPush = (props) => {
  props.handleSummaryPush();
};

// Function to open userprofile popup.

export const handleMyUserPClickOpen = (props) => {
  // props.handleMyUserPClickOpen(true);
};
